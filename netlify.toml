[build]
  command = "hugo --gc --minify -D"
  publish = "public"

[build.environment]
  HUGO_VERSION = "0.55.5"
  HUGO_ENABLEGITINFO = "true"

[context.production.environment]
  HUGO_ENV = "production"
