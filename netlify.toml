[build]
  command = "hugo --gc --minify -D"
  publish = "public"

[build.environment]
  HUGO_VERSION = "0.55.2"
  HUGO_ENABLEGITINFO = "true"

[context.production.environment]
  HUGO_ENV = "production"
